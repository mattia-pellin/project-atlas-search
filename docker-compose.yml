services:
  atlas-search:
    build: .
    container_name: atlas-search
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./credentials.json:/data/credentials.json
