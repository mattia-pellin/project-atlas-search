services:
  atlas-search:
    build: .
    container_name: atlas-search
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./atlas_search.db:/app/atlas_search.db
      - ./credentials.json:/app/credentials.json
      - ./backend:/app/backend
